<%= simple_form_for(user, url: own_user_path, html: { class: 'reg-page' }) do |f| %>
  <%= f.input :firstname %>
  <%= f.input :lastname %>
  <%= f.button :submit, id: 'user-info-submit' %>
<% end %>

