<div class="col-md-6 col-md-offset-3 col-xs-12 reg-page">
  <div class="headline">
    <h1><%= t('menu.edit') %></h1>
  </div>

  <%= render('form', menu: @menu) %>
  <hr>
  <%= link_to(t('menu.index'), admin_menus_path,
                               class: 'btn secondary pull-right') %>
</div>
