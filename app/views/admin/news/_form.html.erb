<%= simple_form_for([:admin, news]) do |f| %>
  <%= f.input :title %>
  <%= f.input :content, as: :text, input_html: { class: 'markdown', rows: 10 } %>
  <%= f.input :url %>
  <%= f.button :submit %>
<% end %>
