<%= simple_form_for([:admin, news]) do |f| %>
  <%= f.input :title %>
  <%= f.input :content, as: :pagedown, input_html: { preview: true, rows: 10 } %>
  <%= f.input :url %>
  <%= f.button :submit %>
<% end %>
