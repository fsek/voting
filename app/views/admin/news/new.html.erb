<div class="col-md-8 col-md-offset-2 col-sm-12 reg-page">
  <div class="headline">
    <h1><%= t('news.new') %></h1>
  </div>
  <%= render 'form'%>
  <hr>
  <%= link_to(t(:index_attr, attr: models_name(News)),
              admin_news_index_path(@news), class: 'btn secondary') %>
</div>
