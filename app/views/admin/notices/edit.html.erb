<% provide(:title, t('notice.title')) %>
<div class="col-md-8 col-md-offset-2 col-sm-12 reg-page">
  <div class="headline">
    <h1><%= t('notice.edit') %></h1>
  </div>

  <%= render 'form', notice: @notice %>

  <hr>
  <%= link_to(t('notice.destroy'), admin_notice_path(@notice), method: :delete,
                                   class: 'btn danger pull-right') %>
  <%= link_to(t('notice.index'), admin_notices_path, class: 'btn secondary') %>
</div>
