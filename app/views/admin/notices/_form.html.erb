<%= simple_form_for([:admin, notice]) do |f| %>
  <%= f.input :title %>
  <%= f.input :description, as: :text %>
  <%= f.input :public %>
  <%= f.input :sort %>
  <%= f.button :submit, id: 'submit-notice' %>
<% end %>
