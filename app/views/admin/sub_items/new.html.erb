<div class="col-12">
  <div class="headline">
    <h1><%= t('.title') %></h1>
  </div>
</div>

<div class="col-12 col-md-3">
  <%= link_to(t('.edit_item'), edit_admin_item_path(@item),
                               class: 'btn btn-secondary') %>
</div>

<div class="col-12 col-md-9">
  <%= render('form', item: @item, sub_item: @sub_item) %>
</div>
