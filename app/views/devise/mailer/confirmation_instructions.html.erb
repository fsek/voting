<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1><%= t('user.mailer.hello', user: @user.to_s) %></h1>
    <%= simple_format(t('user.mailer.registered')) %>
    <%= simple_format(t('user.mailer.before_starting')) %>

    <p>
      <%= link_to(t('user.mailer.please_confirm'), user_confirmation_url(confirmation_token: @token)) %>
    </p>

    <%= simple_format(t('user.mailer.enjoy')) %>
    <%= simple_format(t('user.mailer.problem')) %>
  </body>
</html>
